# Release requirements and current audit

Objective: finish the minimal agent-friendly AI disclosure product, publish it on Henrixbor's public GitHub profile, and deliver a go-to-market plan. A development release does not complete the production objective. Status checked 2026-09-10.

| Requirement | Current evidence | Remaining production work |
| --- | --- | --- |
| Maintained policy, uncertainty and evidence | Dated official sources in skill references; versioned assessment decisions; unknown facts remain unresolved; review tied to revision | Independent legal review; the review package and PDF change-check procedure are prepared but no reviewer has been engaged |
| Actual notices and publishing integration | Static HTML builder; shared component renderer for CMS/server transactions; article/image/chat and local media controls | Framework/CMS adoption tests beyond fixtures; alternate media sources and other track types |
| Existing and future content | HTML candidate inventory, explicit bindings, record hook, media-byte hashes, staging snapshot checks | Historical facts require real evidence; arbitrary structures cannot be proved inventoried automatically |
| First-exposure and accessibility | Chromium, Firefox and WebKit desktop contexts at narrow/wide viewports: no-JS demo notices, chat deep-link/resume, audible-notice sequencing, captions, fullscreen and offline exports | Real spoken notice validation, physical mobile/assistive-technology testing, broader framework/CMS browser coverage, external widgets and voice/native interactions |
| Dynamic publishing | Actual fragment renderer exercised with a recorded update and held stale update, preserving published content | Production framework caching, navigation and asset lifecycle integration |
| Exports and provenance | Portable article/image HTML exports embed original raster bytes and preserve notices offline and in print styling; provider duties remain separate | Raw media, audio/video, PDF/Word and native/social exports; machine-readable provider marking and provenance validation |
| Minimal host integration | One five-step portable skill; project installer; standalone and dual-host archives; validated plugin schemas | Official catalog submissions and host-selection evaluation; no claim of automatic discovery in uninstalled sessions |
| Tests and safe operation | Python 3.9/3.13 Linux/macOS CI; browser job; Python suite and six Node publishing checks, also run from an extracted skill when Node is available | Final verification against deployed customer integrations and remaining modalities |
| Website | Refreshed [owner-private Sites website](https://henrixbor-ai-disclosure.henrixbor.chatgpt.site) with alpha.3 downloads, catalog commands and current scope; deployment succeeded and returned current content | Decide public audience for launch; current access remains owner-only |
| Repository and release | Public [Henrixbor/ai-disclosure](https://github.com/Henrixbor/ai-disclosure); MIT licence, contribution/security guidance; alpha.3 assets downloaded and hash-matched | Complete the remaining production gates before a stable release |
| Distribution | Real Claude catalog installation into temporary configuration, exactly one skill and no agents/hooks/servers; Codex project installer tested | Codex marketplace packaging/listing; official catalog acceptance not obtained |
| Go-to-market | Audience, pilots, positioning, channels, metrics, operating ownership and draft launch copy in `docs/go-to-market.md` | Pilot partners, independently reproduced integrations and approved external promotion |

## Current release

[0.1.0-alpha.3](https://github.com/Henrixbor/ai-disclosure/releases/tag/v0.1.0-alpha.3) is public at source `d1d2a0405ff9cc4e089df7f959f7ef723e62b72d`. All seven CI jobs passed. The skill, dual-host plugin and WordPress archives exactly match clean committed-source builds; anonymous public downloads and SHA256SUMS were byte-matched. Both host manifests validated. The owner catalog now pins the alpha.3 plugin archive; an isolated installation matched all 15 files at the actual installed-registry path, with one skill and no agents/hooks/MCP payload. A prior intermittent Firefox login failure remains unexplained; passing this run is not a root-cause fix.

The development entries below record work leading to alpha.3; their earlier “unreleased” wording is historical. The production gates in the table remain open. The owner-private website now advertises alpha.3; Sites version 3 deployed successfully from `df92e892bcc79629212bcdc1eac2357cce5be577` and returned authenticated HTTP 200 with the updated release and integration descriptions. Its access remains owner-only.

[0.1.0-alpha.2](https://github.com/Henrixbor/ai-disclosure/releases/tag/v0.1.0-alpha.2) is public, tied to source commit `e761ab7203c9b5994f00425fb6bfe785e23f54d1`. Its 46-test suite and all five GitHub jobs passed. Skill/plugin archives were validated, downloaded and hash-matched. Claude's catalog pins the alpha.2 archive SHA-256. Alpha.1 remains available unchanged.

## Completion boundary

The full objective remains open. No independent legal review has been obtained, no official marketplace listing is claimed, and no tool result certifies all website law. Complete the remaining engineering and deployment work, then audit every row with current authoritative evidence. Do not remove a requirement simply because an alpha can be published. Keep the existing Sites project; never recreate it.

Website deployment evidence: source `eedd40aacce1db75e4d550416085865f3a502877`, Sites version 2, successful deployment and authenticated HTTP 200 with current release/scope. That commit’s five CI jobs passed. No public access change has been made.

Unreleased main: portable document export is implemented and browser-tested offline with no external requests. The public catalog remains pinned to alpha.2 until the next validated release; do not describe these main changes as already installed for catalog users.

A [legal review package](docs/legal-review.md) maps the implemented decisions and sample outputs to specific questions for a qualified reviewer. The PDF fingerprint checker does not automate legal judgment. The public website-access question remains pending; the site stays owner-private.

Unreleased WordPress cache adapter: committed assessment records, amendments and withdrawals automatically clear WP Super Cache through its full-clear API. Fresh MySQL/browser checks passed with the SHA-pinned official 3.1.3 plugin: genuine anonymous cache hits, changed notices, withdrawal and first assessment of cached legacy content, with private evidence absent. Coverage is single-site PHP serving with cache rebuild disabled; other modes/providers, CDNs, storage failures and concurrent cache writers remain deployment work. The five-step skill is unchanged.

Unreleased JavaScript integration: a dependency-free Node client calls the same Python renderer through bounded stdin/stdout JSON at publishing time. It supports inspection, rendering and portable export; stale facts and failed operations hold updates. It does not add per-visitor processing or a framework/CMS transaction layer. The skill entrypoint remains unchanged.

The Next.js App Router example now connects preparation to the real build configuration. Its verifier exercises a production static export, no-JavaScript direct visits, client navigation/history, private-output checks, and a stale-content build in an isolated copy. It demonstrates one framework's static publication path; production CMS transactions, broader route inventories and customer adoption remain open.

The experimental WordPress plugin implements authenticated assessment recording, private policy/version records, REST/native update gates, a scheduled-policy check and text notices in content/excerpts/feeds. Its native PHP engine matches Python over 9,253 inputs and requires no Python or remote service on the host. The shared PHP/browser suite exercises WordPress 7.1 with PHP 8.3 on Playground SQLite and pinned Docker MySQL 8.4, including no-JavaScript Chromium views. A separate MySQL CI job records exact runtime versions and collation and coordinates separate database connections through competing amendments and both withdrawal/publication orderings. Conditional publication SQL now prevents the tested withdrawn version from becoming public; a separate anonymous reader receives 404 before corrective hooks run. The status-only core/cron publisher now guards assessment bytes, source, status and schedule dates; its cancellation/withdrawal race is tested with separate workers. Competing archive inserts clear stale missing-key caches before reuse. Explicit amendments preserve private prior records, reject stale database writers and support idempotent retries. Classic and block editor controls bind assessments to current text and require reloading after edits; browser tests exercise recording, amendment, draft withdrawal/restoration and native publication. Withdrawals preserve audit records and block republication until explicit reassessment. A bounded private inventory now discovers post/page source records and their assessment gaps. Evidence-backed historical migration, additional publishing paths/surfaces and broader database/theme verification, alternative publication paths/readers, external caching and concurrent deployment load testing remain open. See `integrations/wordpress/README.md`; this is not a stable plugin release.

Unreleased browser verification: the demo and media/export checks now select Chromium, Firefox or WebKit through one development-only helper; CI runs all three engines. Local runs passed without relaxing existing assertions. Narrow/wide viewport checks are desktop browser contexts, not real-device mobile testing. At that stage WordPress and Next.js browser checks remained Chromium-only; see the expanded WordPress verification below. No new runtime dependencies or agent instructions were added.

The first expanded Linux CI run exposed a missing audio server: Firefox could not finish the disclosure playback. A disposable official Playwright Linux container reproduced it; adding a PulseAudio virtual output made the unchanged Firefox and WebKit media assertions pass. CI now provisions that test-only output, and failure diagnostics retain media error details. No player behavior or playback assertion was relaxed.

Unreleased WordPress distribution: `package_skill.py --format wordpress` builds a reproducible 12-file installable ZIP with an explicit runtime allowlist and matching version/requirement metadata. It excludes development files and private records. Both WordPress runners now build and install that archive instead of mounting source; MySQL verifies its digest and uses WordPress's native `Plugin_Upgrader`. Packaging tests cover reproducibility, source-byte fidelity, runtime completeness, excluded files, missing files and symlinks. Stable publication, WordPress.org acceptance, existing-install upgrades and hosting-specific upload/permissions remain open.

Unreleased WordPress replacement verification: the MySQL/Chromium suite now uses the actual admin upload/confirmation flow. Invalid archives and unwritable staging are rejected with activation, records and files intact. Same-version replacement preserves all private assessment/history bytes, the published revision and its notice, removes an obsolete fixture file and still rejects an unassessed edit. This closes the tested same-version replacement path; version-changing migrations, interrupted upgrades, continuous serving and broader hosting/permissions remain open. No runtime code or agent instructions changed.

The replacement CI checks passed, while two Playground runs exceeded the shared 10-second timeout during initial login/admin navigation. The editor harness now gives navigation a separate 60-second bound; controls retain their 10-second timeout and the same behavior assertions. No failed test is silently retried.

Unreleased WordPress update identity: the plugin now declares the public repository in `Plugin URI` and `Update URI`, using WordPress's documented third-party update identity mechanism. The shared PHP suite intercepts update-check HTTP, verifies the installed header is sent and its hostname hook is invoked, confirms no repository update is invented, and preserves a normal update response for a fictional control plugin. This is an integration check of core dispatch, not a live WordPress.org service test or a package-signature guarantee. Updates remain manual; no remote updater or policy fetcher is installed.

Unreleased migration prerequisite: assessment requests can supply `expected_revision`, and the editor now does so automatically. The server rejects stale saved/proposed versions before creating records, with 400 for malformed revisions and 409 for mismatches. Matching retries retain record identity. The shared tests exercise an actual draft edit between inspection and assessment, proposed-source conflicts and unchanged post content. OpenAPI and the API decisions document describe the additive contract. An evidence-backed batch migration client and complete historical coverage remain open; this precondition does not discover provenance.

The editor script's cache version now hashes its bytes so same-version development replacements load the changed client. Browser checks inspect the script URL and actual assessment POST bodies, verifying that the precondition equals the hash of the submitted title/content/excerpt. The portable five-step skill remains unchanged.

Unreleased WordPress batch migration: a dependency-free repository CLI plans by default and records established facts only with `--apply`. Batches are bounded, credentials come from environment variables, HTTPS is required outside explicit literal-loopback tests, redirects are refused, and private evidence/error bodies stay out of reports. Exact existing records are reused; stale/unknown/conflicting items are held; uncertain writes are reported without automatic retries or rollback claims. Unit tests cover lost-response reconciliation. The MySQL suite exercises the real CLI and application-password authentication, no-write planning, a legacy publication notice, held items and retry identity. Customer evidence, production TLS/authentication and full historical surface coverage remain open.

Unreleased migration discovery: the same repository CLI now traverses bounded authenticated inventory pages and returns resumable cursors with the initial upper ID preserved. Reports contain assessment gaps and revisions but omit title excerpts and never generate origin facts. Empty permission-filtered pages are supported, malformed pages are rejected before retaining their records, and later failures preserve earlier validated results. Discovery is read-only and covers accessible post/page sources, not an immutable snapshot or complete rendered-site coverage. The five-step skill and packaged runtime are unchanged.

Unreleased WordPress browser coverage: both disposable WordPress runners now use the existing strict browser-engine selector. The same classic/block editor, private-record, no-JavaScript notice and publication assertions run in Chromium, Firefox and WebKit; the MySQL admin replacement checks use the selected engine as well. CI installs and runs all three engines with independent fixtures. Local MySQL checks passed in all three engines. This expands desktop engine coverage without changing shipped runtime code or agent instructions; physical mobile devices, assistive technologies, real hosting and additional content surfaces remain open.

Unreleased installed-skill routing: the existing publishing reference now maps static HTML, Node components and WordPress to their integration paths. WordPress links explicitly distinguish repository-only development tooling from bundled/released skill files, require matching policy versions, and connect discovery, evidence-backed migration and future publishing hooks. Fresh temporary Codex/Claude installations and both archive layouts preserve the reference byte-for-byte with unchanged file counts. The five-step entrypoint is unchanged. This proves instruction delivery, not model selection or an autonomous customer deployment.

The first expanded WordPress CI run passed its MySQL job; the next run failed during Firefox login for the admin replacement fixture, before any upload. The available log did not establish whether the form submitted. Shared test-only login diagnostics now report submission, credential-match booleans, HTTP status and structural form state without values, cookies or response bodies. Actions, assertions and timeouts are unchanged; no automatic retry was added. A forced failed-login check confirms a reflected test secret is omitted. This is diagnostic instrumentation, not a claimed root-cause fix. The legal review packet now includes WordPress migration/gates and assessment-history minimisation, access and retention questions.

Alpha.3 release preparation: both host manifests are now 0.1.0-alpha.3, with candidate notes in `docs/releases/0.1.0-alpha.3.md`. Candidate skill, plugin and WordPress archives and checksums are built locally under `dist/alpha.3`; no alpha.3 release or catalog update is claimed yet. The release must be tied to its verified source commit and its public downloads hash-checked. The two tracked Commission PDFs matched their recorded bytes again; operative-law verification remains explicitly unverified. This prerelease is for independent installation/pilot review and does not complete the production objective.

Website alpha.3 update: the existing Sites project now serves version 3. Narrow/wide no-JavaScript checks passed in Chromium, Firefox and WebKit. Hosting reads a dedicated `out` build directory; the validated archive contains only HTML, two stylesheets and hosting metadata, excluding release downloads and macOS resource forks. Existing access was verified owner-only and preserved. Public audience and real customer deployment validation remain pending.

A later CI run failed in Firefox's classic editor while waiting for fields to load; the status still showed the initial prompt. This is distinct from the earlier replacement-login timeout and does not establish a server failure. Test-only diagnostics now retain the last 12 pointer event targets (control names/element IDs, no values), load/fieldset disabled state and inspection response statuses. A disposable official Playwright Linux image passed the shared MySQL/editor/replacement checks using a loopback forwarding shim for Docker Desktop; the initial host-network attempt could not reach its fixture and was cleaned up. Neither intermittent UI failure is claimed fixed, and shipped plugin code and release archives are unchanged.
